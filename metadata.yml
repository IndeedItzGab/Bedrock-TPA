name: BedrockTPA
version: V1.1.2
status: STABLE
message: |
  # BedrockTPA V1.1.2
  - Fixed an issue during teleportation where it cancel the players because it detects the player moved even though they did not.
  - Refactored slash commands cooldown with maps
  - Removed the duplicated message when an operator used `/back` command on someone
  - Implement sound effects