name: BedrockTPA
version: V1.1.1
status: STABLE
message: |
  # BedrockTPA V1.1.1
  - Implemented cooldown on all slash commands
  - Refactored codes and change file's name style to a readable form
  - Improvement with database management and handling teleportations
  - Fixed an issue where a request process is still there after rejoining the game in a short time.
  - Fixed message where the player does not exists replying with player's tpa disabed.